#include <stdio.h>
#include <stdlib.h>

int main(){

	
	return 0;
}

/* Info about trees in general: 

Traversal and evaluation:
	-post-order = left -> right -> self (most common for expression trees)
		-an expression tree is a full binary tree where an arithmetic calculation is represented

	-pre-order = self -> left -> right
	-in-order = left -> self -> right



*/
